# 塩田川 その4 左岸 完成形ビューア

二級河川塩田川 河川改修工事（その4）左岸 完成形の 3D ビューアです。
スマホのブラウザで開いて、部品の切り替え・透過・測点での断面・計測ができます。
Android では現地で 1:1 の AR 表示もできます。

## 中身

```
  index.html              画面と UI
  app.js                  three.js のビューア本体
  sd_model.json           形状と線形と測点（scripts\sd_viewer.py が作る・約 650 KB）
  model.usdz              iPhone/iPad の AR Quick Look 用（実寸 1:1・全体版）
  model_ground.usdz       ★地面基準版（datum 8.200 m・合わせ箱つき・S32）
  AR基準点.csv / .sim     ★現地に設置する基準点6点（TS で出す用・cp932）
  manifest.webmanifest    ホーム画面に追加するための設定
  sw.js                   オフライン用（一度開けば電波が無くても見られる）
  icon-192.png / icon-512.png
```

`sd_model.json` は `scripts\sd_viewer.py` を実行すると作り直されます。
中身は AR モデル（`出力\AR\塩田川_AR_data.json`）＋設計線形＋測点対照表です。

## できること

```
  部品の表示ON/OFF   完成形の面・張ブロック・階段工・かごマット（平面図案／横断図案）など
  透過               面を透かして 基礎・均し・根固め を見る
  測点でスライス     現況測点で切って断面を見る。厚みも変えられる
  測点ラベル         成果簿の杭の位置と名前
  計測               2点をタップ → 水平距離・比高・斜距離
  タップで情報       どこを触っても 測点・離れ・標高・実座標が出る
  現地AR             1:1 で現地に重ねる（Android Chrome）
```

★かごマットは **平面図案（オリーブ）と横断図案（オレンジ）の2つ**が入っています。
どちらが正かは未確定です（引き継ぎ書 §8-c）。現地で見比べて監督員と決めてください。

## ★公開先（公開済み）

```
  現場で開く URL   ★https://sakoken90-boop.github.io/sakohAR_viewer/
  リポジトリ       github.com/sakoken90-boop/sakohAR_viewer（Public・ブランチ main）
  手元のクローン   ★D:\02 .ソフト関係\SakohARviewer\sakohAR_viewer
  Pages の設定     Deploy from a branch ／ main ／ /(root)
```

★AR とカメラは HTTPS でないと動きません。GitHub Pages は HTTPS なので大丈夫です。

QR コードは、上の URL を QR 作成サービスに入れて作ってください。
現場で配るときは、印刷して詰所に貼っておくのが手軽です。

### ★更新するとき

```
  1) DXF塩田川\ で 作り直し.bat を回す（sd_model.json と USDZ 3本ができる）
  2) 変わったファイルを ★手元のクローン（上の D:\…）にコピーする
  3) ★sw.js の 1行目 const V = 'sd-viewer-vN' の N を必ず上げる
     ← 上げないと、端末が古いキャッシュ（前のモデル）を出し続けて更新が反映されない
  4) クローンのフォルダで
        git add -A
        git commit -m "モデル更新（Sxx）"
        git push
     → 1〜2分で Pages に反映される
  5) 端末側は 開き直せば入れ替わる。入れ替わらないときは
     Safari → 設定 → 履歴とWebサイトデータを消去、または ホーム画面のPWAを削除して入れ直す
  ★画面の題名の下「データ ○○」の日時で 入れ替わったか確認できる
```

```
  ★S55／S57 で変わったファイル
     sd_model.json ／ model.usdz ／ model_bank.usdz ／ app.js ／ sw.js ／ ★index.html
     （model_ground.usdz は中身が同じ）
  sw.js  sd-viewer-v16 → ★sd-viewer-v29 に上げてある（app.js の APP_V と そろえる）
  中身   S55  階段部の 基礎コンクリート・均しコンクリートを 階段の下で つないだ。
              階段部の縦帯工（B=250）を足した（S54）
         S57  ★ズームを 指（マウス）の位置に向かって寄るようにした
         S58  ★ズームの ＋ − 全体 ボタンを足した／副題に アプリの版を出した
         S59  ★計測のクリック点を 小さい点にした（画面上の大きさを一定にした）
         S60  ★更新が端末に届かない件。sw.js をネット優先に＋「最新に更新」ボタン
         S61  ★指が空を指しているときも ズームが効くようにした
         S62  ★階段を横切っていた縦帯を取り消した（S54 の入れ間違い）
         S63  ★計測に スナップ（特徴線の節点・線上）を入れた
         S64  ★回転の速さを 注視点までの距離で変えるようにした
         S65  ★階段の幅を 2.000 m 一定にした（上へ行くほど狭くなっていた）
         S66  ★iOS で Variant Launch 経由の 鋲基準 AR を選べるようにした（実験）
         S67  ★SDK キーを組み込み。iOS では設定なしで使える
         S68  ★AR の説明文を「Quick Look／Variant 経由／Android」で出し分けた
         S69  ★Variant Launch が必ず空振りしていたのを直した（待ち方の間違い）
         S70  ★app.js だけ古いものを送っていた事故（v22 に戻っていた）
         S71  ★App Clip への飛び先を 3通り 順に試すようにした
         S72  ★知らせを最優先で待つようにした（VLaunch は 知らせより先に生える）
         S73  ★App Clip の中でも SDK を読まないと xr が生えない（AR が無反応だった）
         S74  ★AR は 人が押さないと始まらない（自動押下をやめた）＋止まらないよう時間を切った
         S75  ★iOS はカメラが ブラウザの背後。AR 中は 下地を透かす（真っ白だった）
         S76  ★AR に入った後の DOM 書き換えは 画面に出ない。入る前に 切り替える
         S77  ★AR に入ったら まず目の前に仮置き＋床が無くても合わせられる＋画面たたきで操作
         S78  ★重ね表示の root を 専用の箱に（body だと 中身が まるごと隠れていた）
         S79  ★いったん立ち止まり 公式デモで 切り分け
         S80  ★three に 参照空間の型を 伝えていなかった（★これで iPhone の AR が動いた）
         S81  ★iPhone で 鋲基準の AR が 動いた
         S82  ★基準の杭を AR に入る前に 選べるようにした／★既定が AR4 だったのを AR1 に
```

### ★見たい所へ寄る（S57／S58）

```
  ★画面の右はし（上下まんなか）に ＋ − 全体 のボタンがある
      ＋ を1回押すと 40% 寄る。8回で 全景 → 断面の見える大きさ になる
      「全体」で 起動したときの画に戻る
      ※ 現地合わせ中と AR 中は 画面を塞がないように隠れる
  ・ピンチ／ホイールは ★指の位置に向かって寄る（zoomToCursor）
    これまでは モデルの真ん中に向かって寄っていたので、
    端の断面を見ているときは いくら寄せても引き戻されて「効かない」感じだった
  ・★指が 空（モデルの無い所）を指しているときは、指ではなく
    ★画面まんなかのモデルに向かって寄る（S61）。
    この堤防は 195 m の細い帯なので 指が空を指すことが多く、
    そのまま指に寄ると 何もない所へ進んで モデルが横へ逃げていた
  ・★見たい所を ダブルタップ すると そこが回転とズームの中心になる
    「ここを中心にしました」と出たら 成功。そのまま回すと その点のまわりを回れる
```

### ★iPhone / iPad で 鋲基準の AR を使う（S66・実験）

```
  iOS の Safari は WebXR に対応していないので、既定では
  ★AR Quick Look（USDZ・下端が床基準）で開く。高さが合わない。
  ★Variant Launch を経由すると、App Clip の中で 本物の WebXR が動き、
    Android と同じ ★鋲基準（AR1〜AR10）の AR が使える。
    使っている機能は すべて対応済み  hit-test ／ dom-overlay ／ anchors
```

```
  ★準備は済んでいる（S67）。SDK キーは app.js の VL_KEY に組み込み済みで、
    iPhone / iPad では ★はじめから「入」。設定も インストールも要らない
  使い方
    そのまま「AR（実寸）」を押す → Launch Card が出る → 「Open App Clip」
    → Variant のビューアで開き直され、★そのまま現地 AR に入る
    以降は Android と同じ。基準の杭 → ① 足元に合わせる → ② 向きを合わせる
  ★Quick Look に戻したいときは 表示 →「AR（iPhone / iPad・実験）」の目を外す
```

```
  ★「AR（実寸）で使うモデル」の 天端／水際／全体 の選択は
    ★Quick Look のときだけ効く。Variant 経由 と Android では使わない。
    どちらの状態かは その欄の ★説明文に出る（S68）
```

```
  ★ページを開いただけでは SDK を読み込まない（AR を押したときだけ）。
    スクリプトに ★redirect=true は付けていない。付けると iOS の人が
    ページを開いた瞬間に Launch Card へ飛ばされ、3D を見たいだけのとき邪魔になる
  ★SDK キーは ドメインに紐づく ★公開キー。登録した sakoken90-boop.github.io
    以外では使えないので、リポジトリに入っていて差し支えない
  ★無料枠では Launch Card に Variant のブランドが出る（消すには Pro）
  ★外部サービス頼みなので 本線は Android。iOS は補助と考える
```

#### ★S69　「Variant Launch を開始できませんでした」が出ていた件

```
  ★こちらの待ち方の間違い。ドメインの登録とは 関係なかった。
    launchar.app の SDK は ★読み込み終わった時点では まだ VLaunch を作らない。
    中で 非同期に初期化して、終わってから window に
      ★vlaunch-initialized  を投げてくる（detail に webXRStatus など）
    S67 は 読み込み終わり（onload）で VLaunch を見ていたので ★必ず空振り。
    8 秒の保険も 先に「駄目」で片が付いた後なので 効いていなかった。
  → ★vlaunch-initialized を待つようにした。
  → 駄目だったときは ★何が駄目だったかを 画面に出す
      「SDK を読み込めませんでした」        通信／キー
      「SDK が初期化されませんでした」      ★ドメイン登録（8 秒待って出る）
  → webXRStatus が unsupported の端末は ★黙って Quick Look に回す（断らない）
  ★それでも駄目なとき  launchar.app の管理画面で ドメインが
      sakoken90-boop.github.io
    の形で 登録されているか（★省略なしの全部）。先方の決まりで 完全一致が要る
```

#### ★S71　飛び先を 3通り 順に試す

```
  ★ドメイン登録は通った（SDK が初期化まで行った）。次は
    「飛び先を作るときに止まりました」＝ ★getLaunchUrl() が こけた。
  飛び先には ★3つの道がある
    ★① getLaunchUrl(いまのURL + ?vlxr=1)
         ★いちばん良い。開き直した先で ★そのまま AR に入る（自動で押す）
    ★② 知らせ（vlaunch-initialized）の中の launchUrl
         いまのページを 開き直すだけ。★開いた先で もう一度 AR を押す
    ★③ directAppClipUrl   App Clip へ直に（30 分で切れる）
  S69 までは ①しか見ていなかったので、①がこけると そこで終わっていた。
  ★S71 で ①→②→③ の順に落ちるようにした。
  ②③で飛んだときは 画面下に ★「開いた先で もう一度 AR を押してください」と出る。
```

```
  ★駄目だったときの お知らせに 状態を出すようにした
    例  状態：WebXR App Clip が要る／知らせ あり／getLaunchUrl あり／launchUrl なし
    この4つを 教えてもらえれば どこで止まっているか こちらで分かる
```

#### ★S72　知らせを 最優先で待つ（★VLaunch は 知らせより 先に生える）

```
  現場から届いた状態
    ★知らせ なし ／ getLaunchUrl あり ／ launchUrl なし
  ★これで はっきりした。SDK は
      ① window.VLaunch を作る（getLaunchUrl も 生える）
      ② ★そのあと 中身を揃えて vlaunch-initialized を投げる
    の順で動く。①と②の間の getLaunchUrl は ★呼ぶと止まる。
  S71 は「VLaunch が生えたら すぐ使える」と思って ★①の時点で呼んでいた。
  → ★S72  知らせを 最優先で待つ。
     知らせが来なかったときだけ ダメ元で VLaunch を使う。
  → ★さらに 飛び先ができるまで 最大 6 秒 粘る（vlTry）。
     粘っている間に 知らせが来れば ①でも②でも 使えるようになる
  ★押してから最大 14 秒かかることがある。ボタンは「準備中…」のまま
```

#### ★S73　App Clip の中で AR が 無反応だった

```
  Launch Card →「Open App Clip」→ ★Variant のビューアで開き直すところまでは
  うまくいっていた。そこで 緑の「AR」を押しても ★何も起きなかった。
  ★理由  App Clip の中でも、★ページが SDK を読み込まなければ
         navigator.xr は生えない。WebXR を注いでいるのは ★SDK 自身だから。
         S66 で「SDK は 押したときだけ読む」ようにしたので、開き直した先では
         xr が無い → isQuickLook() が true → AR を押すと Quick Look に回り、
         ★App Clip の中では USDZ も開かないので ★無反応に見えていた
  → ★?vlxr=1 が付いていたら 開いた時点で SDK を読み、xr が生えるのを待つ
       画面下に「現地 AR の準備をしています…（10 秒ほど）」と出る
       生えたら ★自動で AR に入る
  → ★AR ボタンは 押すたびに 判定をやり直す（開いた時の判定を信じない）
  → ★Variant の中では もう Launch Card へ飛ばさない（堂々めぐりを断つ）
  → 一度 xr が駄目だったら ★二度目からは待たずに Quick Look に回る
```

#### ★S74　「現地 AR を開きます…」で止まっていた

```
  S73 で xr は 生えるようになった。が、そのあと ★自動で AR ボタンを押す所で
  止まっていた。
  ★理由  WebXR の immersive セッションは ★人が押した操作からしか 始められない
         （user activation が要る）。setTimeout の中の click には それが無いので、
         requestSession が ★返ってこないまま 止まる。
  → ★自動で押すのを やめた。準備ができたら
       画面下「★準備ができました。上の『AR（実寸）』を押してください。」
       ボタンも ★「▶ AR（実寸）」に変えて 目立たせる
  → ★黙って止まらないように 時間を切った
       対応の問い合わせ  6 秒
       AR の開始        25 秒
     切れたら ★「○○が ○秒 返ってきませんでした」と出す
```

#### ★S75　AR には入ったのに 画面が 真っ白だった

```
  ★AR は 始まっていた（iPhone の上に ★カメラ使用中の緑の点 が出ていた）。
  なのに ★カメラ映像が見えず 真っ白。
  ★理由  iOS（Variant のビューア）では カメラ映像を ★ブラウザの背後 に描く。
         ページが 不透明だと そこで隠れる。
         こちらの CSS は  html,body{background:var(--bg)}  ＝ ★不透明。
         先方の注意書きにも
           「all page elements and the canvas itself are set to transparent」
  → ★AR 中だけ html と body の下地を 透かす（class="arx"）。
     UI（ヘッダー・パネル）は そのままでよい（dom-overlay で上に乗る）
  → ★AR を終えたら 元の色に戻す
  ついでに
  ★AR の開始を 4段階に分けて 画面に出す（止まった所が すぐ分かる）
      ①カメラの起動 ②描画の引き渡し ③基準の空間 ④床の検出
  ★基準の空間は local → local-floor → viewer の順に試す
  ★床の検出（hit-test）が使えなくても AR には入れるようにした
      その場合 ① は ★カメラの 1.5 m 前・1.2 m 下 を仮に使う（精度は落ちる）
```

#### ★S76　AR に入った後の 書き換えは 画面に出ない

```
  S75 で入れた「★AR を開始しています…①」が ★一度も出なかった、という報告で
  はっきりした。
  ★Variant の dom-overlay は 本物ではなく
      「root に指定した要素 以外を 隠す」まね事 で作られている
      （先方の注意書き  DOM Overlay support is ★emulated …）
  そのため ★セッションに入った後の DOM の書き換えが 画面に出ないことがある。
  S75 の「AR 中に 下地を透かす」も ★同じ理由で 効いていなかった。
  → ★下地は はじめから 透明にした（html,body{background:transparent}）
     ふだんの背景色は ★3D 側（scene.background）が描いているので 見た目は変わらない
  → ★AR の見た目は セッションを始める ★前 に まとめて切り替える（arSkin）
       下地・ズームUI・AR用UI・ボタンの文字（AR終了）・案内文 を 先に入れる
       しくじったら 元に戻す
  → ★段階の記録は ためておき、AR を抜けた後に まとめて出す
       画面に出せないので ★終わってから見せる
```

#### ★S77　カメラは映った。次は「モデルが どこにあるか 分からない」

```
  ★カメラが映った（S76 の読みが 当たった）。
  次は ★モデルが 見当たらない。
  ★理由  AR に入った直後は モデルが ★AR の原点（セッションを始めた所）に置かれる。
         この堤防は 195 m の細い帯なので、たまたま 視界の外にあると
         ★どこを向いても 何も見えない。
  → ★AR に入ったら まず ★目の前 3 m に 仮置きする。
       選んでいる基準点（AR1 など）が ちょうど 目の前 3 m・1.2 m 下 に来る。
       ★とにかく 見える。そのあと 杭の所で ①を押せば 正確に合う
  → ★床の検出（白い輪郭）が出ていなくても ①②が 押せるようにした
       ① 輪郭が無ければ ★前 1.5 m・1.2 m 下
       ② 輪郭が無ければ ★いま向いている方向の 5 m 先
  → ★逃げ道　AR 中は ★画面を たたいても 操作できる
       ★1回 → ①足元に合わせる ／ ★2回 → ②向きを合わせる
       （下のボタンが 見えない端末のため）
```

#### ★S78　モデルも ボタンも 出ない ―― root が body だったため

```
  v37 の結果  ★カメラは映る。★モデルは見えない。★①②のボタンも見えない。
  ★読み  Variant の dom-overlay は「★root 以外を隠す」まね事。
         root に ★document.body を渡していたので、
         ★body の子（3D の画＝canvas、ヘッダー、AR用UI）が まるごと隠れ、
         ★カメラだけが 残っていた。
         先方も「フレームワークが触らない ★専用の箱 を root に」と書いている。
  → ★AR の間だけ、canvas と AR用UI を ★ひとつの箱（#arov）に入れて、
     ★その箱を root にする。AR を抜けたら 元に戻す
     （箱に入れても WebGL の画は そのまま。PC で確かめた）
  → ★仮置きは arFrame を待たずに ★セッションの直後に 置く
       local の原点は 始めた時の端末なので (0, -1.2, -3) ＝ 目の前 3 m・1.2 m 下
  → ★画面タップは ★XR の select で拾う（dom-overlay が効かなくても 必ず届く）
       ★1回 → ①足元に合わせる ／ ★2回（0.6 秒以内）→ ②向きを合わせる
  → AR用UI に ★「終了」ボタンを足した（ヘッダーが隠れても 抜けられる）
```


### ★回転の速さ（S64）

```
  OrbitControls の回転は「画面の高さいっぱいのドラッグ ＝ 約180°」で、
  ★カメラが近いか遠いかに関係なく 同じ角度だけ回る。
  寄っているときは 画面に写る範囲が狭いので 振れ幅が大きく感じる。
  → ★注視点までの距離で 回転の速さを変えるようにした

  実測（24 px ドラッグで 画面が何 px 動くか）
      引いた画   26 px → ★10 px
      寄った画   90 px → ★21 px   （指の動きと ほぼ 1 対 1）

  速さを変えるときは app.js の 3 つの数字
      ROT_FAR  0.60   引いているとき
      ROT_NEAR 0.15   寄っているとき
      ROT_D    60     この距離（m）以上は ROT_FAR のまま
```

### ★計測のスナップ（S63）

```
  「計測」を押して 2点をタップすると 水平・比高・斜距離が出る。
  ★タップした所の近くに 特徴線の節点があれば そこに吸い付く（画面上 22 px 以内）
      節点 ＝ ★出来形の計測対象点そのもの
        法肩 ／ 天端頂点 ／ 天端外縁 ／ 管理用道路_表層端・路盤端 ／
        法尻_河床端 ／ 川裏法尻 ／ 大型張ブロック・縦帯・基礎・均し・根固め・
        階段・横帯・かごマット の角
  ★節点が無ければ 線の上（垂線の足）に吸い付く（14 px 以内）
  ★吸い付いた点は ★水色の印。吸い付かなかった点は 赤
  HUD に 「★節点 法肩」のように 何に吸い付いたかが出る
  ★外すとき  表示 →「計測」→ スナップ の目を外す（面の好きな所を測れる）

  ★効きめ  面をそのまま拾うと 三角形のどこかになり 離れが 9.08 や 9.12 とばらつく。
          スナップすると ★離れ 9.100（法肩）／11.850（表層端）／2.500（法尻）と
          設計値ちょうどが出る
```

### ★入れ替わったか確かめる／入れ替わらないとき

```
  題名の下に ★「アプリ v29」 と出る。ここが上がっていなければ 古いまま
```

```
  1) まず 開き直す
  2) それでも上がらないなら ★「表示」→ 一番下の「更新」→
     ★［最新に更新（キャッシュを消して読み直す）］を押す
     → キャッシュと Service Worker を消して 読み直す。
       ★現地で入れた基準点の標高（localStorage）は 消えない
  3) ボタンが見当たらない（そもそも古い版）ときは
     ・★プライベートブラウズ（シークレット）で URL を開く
       ここで新しい版が出れば サーバは正常。端末のキャッシュの問題と分かる
     ・ホーム画面の PWA を削除 → Safari で開き直して 追加し直す
     ・または Safari → 設定 → 詳細 → Web サイトデータ →
       sakoken90-boop.github.io を削除（全消しでなくてよい）
```

```
  ★S60 で sw.js の考え方を変えた
     コード（index.html / app.js）  ★ネット優先（2.5 秒で諦めてキャッシュ）
     データ・モデル（sd_model.json・USDZ）  キャッシュ優先＋裏で取り直す
  → 直したコードが 開き直し1回で 端末に届く。
    圏外では キャッシュに落ちるので 現場での動きは これまでどおり
  ★app.js／index.html を直したら sw.js の版と APP_V も必ず上げる
```

## ★★どちらを使うか（2つは独立。つなげて使うものではない）

```
  ★「現地合わせ」と「AR（実寸）」の間で データは一切やりとりしていない。
    現地合わせをしてから AR（実寸）を押しても 何も引き継がれない。
    ★AR（実寸）を使うなら 最初から AR（実寸）だけでよい
```

```
                    現地合わせ                    AR（実寸）＝Quick Look
  中身              自前のカメラ重ね              iOS 標準の AR
  平面検出          使わない                      使う（床を探す）
  立ち位置          ★基準点の鋲の上に立つ         どこでもよい
  合わせ方          十字＋ルーペで鋲を視準         ★指で動かす（1本指・2本指）
  歩けるか          ★歩けない（動くとずれる）      ★歩ける（ARKit が保つ）
  精度              ★高い（他点 0.02〜0.35 m）     目分量（0.1〜0.3 m くらい）
  高さ              ★絶対値（鋲の標高から計算）    指で上げる（目分量）
  部品の表示切替    ★できる                       できない（★Quick Look に UI が無い）
  実物の陰          ★隠れない（映像の上に描くだけ） ★隠れる（オクルージョン）
  向いている用途    ★一点から 納まり・干渉を精査   ★歩いて全体像・監督員に見せる
```

```
  ★AR（実寸）で部品を切り替えられないのは「触るとずれるから」ではない。
    Quick Look は Apple が UI を固定していて ★機能そのものが無い。
    USDZ 側にも レイヤ／タグの概念が無い（§S36）
  ★AR（実寸）でモデルが実物の後ろに消えるのは ★オクルージョン（遮蔽）。
    ・人物セグメンテーション（A12 以降・★自動・切れない）
    ・LiDAR 搭載機（iPhone Pro など）は 現実の奥行きでも遮蔽する
    正しい挙動だが、奥行きの推定が粗いので ★本来隠れない所まで消えることがある
    近づきすぎると 手前の面が切れて消えることもある（ニアクリップ）
    → 少し離れる／手や体を画角から外す。
      ★草や機械の陰でも設計形状を見たいときは 現地合わせ（遮蔽が無い）
```

```
  ★どちらを使うにも 先にやること
    ・鋲を出す（AR基準点.sim を TS／GNSS に入れて AR1〜AR10）
    ・★鋲の頭の標高を実測する
        現地合わせ用  画面の「基準点の標高」に入れる（端末に保存）
        AR（実寸）用  ★出力\AR基準点_実測Z.csv に書いて USDZ を作り直す
          （合わせ十字は 基準点の標高に描いてある。標高が計画値のままだと
            十字も その高さに出るので 0.2〜0.5 m ずれる）
```

## ★現地合わせ（基準点2点方式）── 精度が要るときはこちら

Quick Look は数値入力ができず位置合わせが目分量になるので、**現地に基準点を6点
設置して、そこに合わせる方式**を用意しました。ビューアの「現地合わせ」ボタンです。

### 準備（一度だけ）

```
  1) AR基準点.sim を TS に入れて、6点を現地に出す（鋲・杭）
     天端頂点（設計中心線から離れ 10.600）の上、39 m 間隔
     点名  設計追距    X(北)       Y(東)      計画天端高
     AR1   1914.000  84708.883  78794.384   8.427
     AR2   1953.000  84711.636  78756.179   8.557
     AR3   1992.000  84716.817  78718.054   8.687
     AR4   2031.000  84722.781  78679.513   8.817
     AR5   2070.000  84728.744  78640.972   8.947
     AR6   2109.000  84737.282  78605.926   9.078
  2) ★打った杭の頭の標高を実測する
  3) ビューアの「現地合わせ」→「基準点の標高」に実測値を入れて保存
     （端末に保存されます。計画天端高のままだと上下にずれます）
```

### ★2つのモードは別物（混同しやすい）

```
  「現地合わせ」  自前のカメラ重ね。★平面検出は無い（使っていない）。
                  基準点に立って 方位を解く。高さは基準点の標高で決まる＝★絶対値
  「AR（実寸）」  iOS 標準の AR Quick Look。★平面検出がある（床を探す）。
                  ★高さは「検出した床」基準。モデルの最下端が床に乗る＝★相対値
  → 「iPhoneを動かして開始」の案内が出るのは AR（実寸）だけ。現地合わせには出ない
```

### 現地での手順

```
  1) 基準点のどれかに立つ
  2) 「現地合わせ」を押す（カメラと「動作と方向」の許可を出す）
  3) 「立っている点」を選ぶ／★「カメラの高さ」を入れる
     ＝ 杭の頭から ★カメラのレンズまで の高さ。目の高さではない。
       三脚なら 雲台＋端末の取付位置まで含めて実測する（±1 cm でよい）
  4) 「狙う点」で別の基準点を選び、★画面中央の十字にその杭を重ねて「記録」
  5) これで完成形がカメラ映像に重なる。見回すと追従する
  6) さらに別の基準点で「記録」すると、前の点とのずれ量が出る（合わせの検算）
```

### 精度

立っている点は測量で出した既知点なので**カメラ位置は正確**、上下と傾きは重力
センサ（正確）、**方位だけが未知**です。十字に重ねて記録すると、その向きが
ちょうどその点を向くように姿勢を補正するので、画角に関係なく決まります。

```
  計算上の検算（センサのピッチ誤差 0.5° を与えた場合の残差）
    AR1 に立ち AR2 で合わせ → AR3 0.06 m ／ AR4 0.14 m ／ AR6 0.35 m
    AR6 に立ち AR1 で合わせ → 他点は 0.02 m 以内
  方位の誤差が横ずれに効く量
    0.2° ずれ →  39 m 先 0.14 m ／ 100 m 先 0.35 m ／ 195 m 先 0.68 m
    0.5° ずれ →  39 m 先 0.34 m ／ 100 m 先 0.87 m ／ 195 m 先 1.70 m
```

```
  ★歩くとずれます（カメラ位置を立っている点に固定しているため）。
    移動したら、その点を選び直して合わせ直してください。
  ★画角は端末ごとに違います。基準点6点はほぼ一直線（開き角 2〜5°）なので
    計算では決まりません。初回だけ「画角」スライダーで、
    手前の法肩線などが実物と合うところに調整してください（端末に保存されます）。
  ★出来形の判定には使えません。形・納まり・干渉の確認までです。
```

### ★AR基準点の表示／非表示（S35）

```
  ビューア      「表示」→「測点ラベル」に ★「AR基準点（赤いポールと名前）を出す」
                を追加。チェックを外すと ポールも名前も消える
                ★現地合わせ中は、立っている点のポールだけ自動で隠れる（S30）
                モデル側の部品「AR基準点」（USDZ／SketchUp 用）は
                「部品の表示」→ ★「その他」に出る。既定は OFF
  SketchUp      タグ「AR基準点」で切替できる
  ★Quick Look   切替できない（レイヤの機能が無い）。
                ポールと合わせ箱は 位置合わせの道具なので入れてある
```

### ★S29 で入れたもの（精度を上げる3点＋印刷）

```
  1) ★ルーペ  十字のまわりを 2/3/5/8 倍で拡大表示する。
     指し誤差が 0.03° → 0.01° 級まで下がる。★一番効く
     「現地合わせ」パネルの下にチェックと倍率がある

  2) ★複数点の平均  記録を1点で上書きせず、記録した全点の
     方位・仰角を平均して姿勢を作る（最小二乗）。
     ★1点だけ 0.2° 指し損ねても、5点の平均なら方位の誤りは 0.04° に薄まる（検算済み）
     ・同じ点をもう一度記録すると 上書きになる
     ・2点目以降は「最大残差 ○○°（○○ m 先で ○○ m）」が出る。合わせの検算になる
     ・「狙う点」は 記録するたびに ★まだ記録していない中で一番遠い点 に自動で進む

  3) ★タップした点でも記録できる  画面のどこかをタップすると その向きで記録する
     （十字に重ねなくてよい）。ただし指の精度は ±0.3° 程度なので、
     ★精度が要るときは 十字＋ルーペ

  4) ★印刷（PC 向け）  「表示」パネルの一番下「この画面を印刷」。
     いま見えている画面を A4 横で印刷（PDF 保存も可）。
     工事名・範囲・断面位置・表示中の部品・座標系・日時の表が付く。
     現地合わせ中に押すと カメラ映像も一緒に写る
```

### ★記録の進め方（S31：チップで見える）

```
  1) 「狙う点」は 最初から ★一番遠い点（AR6 など）が入っている。
     そのまま その杭を十字に重ねて「十字に重ねて記録」を押す
  2) 記録すると チップの AR6 が ★緑＋✓ になる。
     「狙う点」は ★自動で 次に遠い未記録の点 に進む（AR5 → AR4 …）
     ★元に戻っているように見えるのは、その点をまだ記録していないから
  3) 2〜3点も記録すれば十分。「○点の平均で合わせ済」と
     「最大残差 ○○°（○○ m 先で ○○ m）」が出る
  4) 手で選び直したいときは ★チップを押す。同じ点をもう一度記録すると上書き

  チップの色  緑＋✓＝記録済 ／ 赤枠＝いま狙っている点 ／ 灰色（立）＝立っている点
```

### ★狙う点は「一番遠い点」を選ぶ

```
  実測（下の精度の節）
    AR1 に立ち ★AR2（38 m）で合わせ → AR6 で 0.35 m ずれた
    AR6 に立ち ★AR1（191 m）で合わせ → 他点は 0.02 m 以内
  → 逆算すると 指し誤差 ≒ 0.03°／立ち位置の誤差 ≒ 0.07 m 相当。
    ★立ち位置の誤差が支配的。近い点で合わせると それが方位の誤りに化ける
      （p / D。D=38 m なら 0.07 m が 0.11°、D=191 m なら 0.02°）
  ★対策  ・狙う点は 見えている中で 一番遠い点にする
          ・三脚で 鋲の真上に据える。カメラの高さ（レンズまで）も実測する
```

### ★「2点を十字で指定して どこに立ってもよい」方式は この現場では使えない

```
  未知数  現行方式        方位だけ（1個）。位置は「点に立つ」ことで確定している
          2点指定方式    X・Y・Z・方位（4個）を 2点の画面指定（4観測）で解く
                         ＝ ちょうど決まるが、★条件が最悪
  なぜ    距離を決めているのは「鉛直角」。ここの基準点は ほぼ一直線・ほぼ同高
          AR1→AR6  水平 190.59 m ／ 高低差 0.651 m ／ ★鉛直角 0.1957°
            鉛直角の指し誤差 0.01° → 距離が  9.3 m 狂う
                              0.03° →       25.3 m
                              0.05° →       38.8 m
          さらに 6点は AR1 から見て 開き角 4.5° 以内。水平の交会も弱い
  ★2点指定が効くのは「距離が大きく違う・水平角が 60〜120° 開く・高低差がある」場合。
    この現場は3つとも逆。どこに立ってもよくしたいなら
    基準点を散らして3点以上（川裏側・法尻側にも打つ）か、GNSS で位置を入れる
```

### 歩いたときのずれ量（AR1 に立って合わせ、天端を上流へ歩いた場合）

視差だけのずれなので、**近くを見るほど大きく、遠くを見るほど小さい**。

```
  狙う所                     2m歩く   5m歩く  10m歩く  20m歩く
  足元の法尻・法肩（〜10 m）   2 m     5〜6 m  11〜13 m  24〜27 m   ★使えない
  20 m 先の法尻              0.9 m    2.2 m    4.5 m   10.5 m
  20 m 先の法肩              0.2 m    0.6 m    1.2 m    3.4 m
  50 m 先の法尻              0.4 m    0.9 m    1.8 m    3.6 m
  50 m 先の法肩              0.1 m    0.2 m    0.4 m    0.7 m
  100 m 先の法肩             0.1 m    0.2 m    0.3 m    0.6 m
  上流端（190 m 先）         0.2 m    0.4 m    0.9 m    1.7 m
```

```
  目安  ずれ ≒ 歩いた距離 × sin(歩いた向きと 見ている向きの角度)
        → 進行方向の遠景はあまり狂わない。真横・足元は歩いた分そのままずれる
  運用  ・足元の納まりを見るときは その基準点から動かない
        ・5 m 以上動いたら合わせ直す（基準点は 39 m 間隔）
        ・遠景の全体像だけなら 20 m 程度動いても 1 m 前後
        ・しゃがむ／立つでカメラの高さが変わったときも同じ。入れ直す
  ★歩き回って見たいときは iPhone の「AR（実寸）」（Quick Look）の方が向いている。
    ARKit が位置を追うので歩いても保つ。ただし初期配置は目分量になる
```

## ★モデルに入っている AR基準点のポール（S26）

モデルの中に **AR1〜AR6 のポール（赤）** が入っています。**ポールの先端（下端）が
基準点そのもの**です。AR1 だけ 3.0 m、他は 2.0 m にしてあるので下流端が分かります。

Quick Look も SketchUp Viewer も ARKit で歩いてもずれませんが、**置く位置は目分量**です。
このポールを現地の鋲に合わせれば、そこが埋まります。

```
  1) AR基準点.sim を TS に入れて AR1〜AR10 の鋲を出しておく（天端6点＋法尻4点）
  2) Quick Look（または SketchUp Viewer）でモデルを置く
  3) ★AR1 のポールの先端を AR1 の鋲に乗せる（ドラッグ）
  4) ★AR2 のポールが AR2 の鋲に重なるまで回す（2本指）
  5) あとは触らない。歩いても ARKit が位置を保つ
```

```
  精度の目安  鋲に ±0.1 m で乗せられれば 方位 ±0.15°
              → 195 m 先で ±0.5 m ／ 39 m 先で ±0.1 m
  ★39 m 離れた2点で合わせるので方位が効く。1点だけだと回転が決まらない
  ★「現地合わせ」（十字方式）の方が精度は上（他点 0.02〜0.35 m）だが歩けない。
    歩いて見る → ポール方式 ／ 一点から精度よく見る → 現地合わせ、で使い分ける
```

### ★鋲の標高を実測したら（S42）

```
  1) レベルで AR1〜AR10 の ★杭頭標高（T.P.）を測る
  2) ★出力\AR基準点_実測Z.csv を Excel／メモ帳で開き、
     ★2列目「実測標高」に 測った値を入れて保存
        点名,実測標高,参考(いまの値),備考
        AR1,8.211,8.427,天端頂点
        AR2,,8.557,天端頂点        ← 空のままなら 参考の値のまま
  3) ★作り直し.bat をダブルクリック（DXF塩田川\ の直下）
     sd_ar.py → sd_viewer.py → USDZ 3本 を まとめて作り直す
  4) 出力\Viewer\ の中身を GitHub Pages に上げ直す
     sd_model.json ／ model.usdz ／ model_ground.usdz ／ model_bank.usdz
  5) ★画面の題名の下「データ ○○」の日時で 入れ替わったか確認する
```

```
  ★これ1回で 両方に効く（S42）
    AR（実寸）    合わせ十字とポールが 実測した鋲の高さに乗る
    現地合わせ    「基準点の標高」の既定値が 実測値になる
                  ＝ ★端末ごとに入力しなくてよくなった
    （端末の画面で入れた値があれば そちらが優先。「元値に戻す」で戻る）
  ★datum も見直す  実測した地盤に合わせるなら bat の中の 8.20／5.50 を書き換える
```

### 標高を実測値にする### 標高を実測値にする

ポールの先端の Z は **計画天端高**です。鋲の頭の実測標高を入れると上下も合います。

```
  出力\AR基準点_実測Z.csv  を作る（無ければ計画値のまま）

    点名,標高
    AR1,8.431
    AR2,8.560
    …

  そのあと
    python scripts\sd_ar.py
    python scripts\sd_viewer.py
    node   scripts\sd_usdz.mjs 出力\Viewer\sd_model.json 出力\Viewer\model.usdz
```

```
  ※ USDZ を作るには DXF塩田川\ で一度 npm i three@0.160.0 が要ります（導入済み）
  ※ ビューアの「現地合わせ」側の標高は、画面の
    「基準点の標高（現地実測値を入れる）」に入れれば端末に保存されます（別管理）
```

## 現地 AR の使い方（iPhone / iPad）── ざっと見る用

右上の「AR（実寸）」を押すと、iOS 標準の **AR Quick Look** が開きます。
ARKit のトラッキングがそのまま効くので、置いたあと歩き回っても位置が保たれます。

```
  1) Safari で開いて 右上の「AR（実寸）」を押す
  2) 「AR」タブに切り替える（Quick Look の画面上部）
  3) 地面を映すと自動で置かれる。1本指のドラッグで移動、2本指で回転
  4) 現地の杭や既設構造物に合わせる
```

```
  ★実寸（1:1）です。延長 195 m あるので、画面には一部しか入りません。
    見たい場所（階段工・かごマットなど）に立って、その付近に合わせてください。
  ★モデルは「平面の中心を原点、一番低いところを地面」に置いてあります。
  ★かごマットは 平面図案（オリーブ）と横断図案（オレンジ）が両方入っています。
  ★位置合わせは手動です。座標での固定ではないので、出来形の判定には使えません。
```

`model.usdz` は `scripts\sd_usdz.mjs` で作り直せます（ネットにつながる PC で
`npm i three@0.160.0` のあと `node sd_usdz.mjs ..\出力\Viewer\sd_model.json ..\出力\Viewer\model.usdz`）。

## ★iPad（第9世代）で使うとき

```
  iPadOS 26 対応・A13。AR Quick Look も「現地合わせ」も動く
  ★LiDAR が無い（iPad Pro とは違う）
  ★GPS は Wi-Fi+Cellular モデルだけ。ただしこの仕組みは GPS を一切使わないので関係ない
  ★方位（コンパス）も使っていない。十字合わせで方位を解く方式なので影響なし
  8MP f/2.4 広角のみ／10.2インチ・487 g
```

### iPad 9 では「現地合わせ」（十字方式）が本命

```
  ・十字方式は ARKit を使わない。カメラ＋重力センサと幾何計算だけなので
    ★LiDAR の有無で精度が変わらない。iPad Pro と同じ精度が出る
  ・★大画面のぶん、十字に杭を重ねるのが iPhone よりやりやすい
  ・★三脚か一脚に載せること。487 g を片手で構えて十字に重ねるのは無理。
    カメラの高さも固定できるので 入力値と実際が合う（＝精度が出る）
```

### セルラーモデルだったら何か変わったか（結論：変わらない）

```
  セルラーモデルで増えるのは GPS/GNSS と 単体での通信 だけ
  ★GNSS は単独測位で水平 3〜10 m・標高はその 1.5〜2 倍。
    AR の位置合わせは cm〜dm の話なので、精度には一切使えない
  ★使えるとすれば「今どの基準点の近くにいるか」を自動で選ぶ程度。
    基準点は 39 m 間隔なので当たりはするが、手で選ぶのと変わらない
  ★通信は iPhone のテザリングで足りる。事前キャッシュしておけば現地では不要
  ※外付けの RTK 受信機を web から読むことは iOS ではできない。
    cm 精度が要る場面は TS／GNSS 測量で。AR は形・納まり・干渉の確認まで
```

### ★地面基準版の USDZ ＋ 合わせ箱（S32）── Quick Look でも XYZ を決める

Quick Look は「モデルの一番低い所」を検出した床に乗せる。
★ならば モデルの最下端の標高 ＝ 置く地面の標高 にしてしまえばよい。

```
  model.usdz          全体版（河床から天端まで。★高さは合わない）
  model_ground.usdz   datum 8.200 m より下を切り落とした版（0.27 MB）
  model_bank.usdz     datum 5.500 m 版（0.59 MB）

  ★どの版にも「合わせ十字」が入っている（S37）
    地面に置く十字の的。腕 ±1.000 m（★AR1 だけ ±1.500 m ＝ 下流端の目印）
    幅 0.040 m ／ 厚み 0.010 m ／ ピンク
    腕は 設計中心線の 測点方向・直角方向 を向いている（向き合わせにも使える）
    ★立方体の一辺に合わせるより、細い腕の交点を鋲に重ねる方が精密
      （測量の対空標識と同じ考え方）。目分量で ±0.05 m → ★±0.01〜0.02 m
```

使い方

```
  1) 「表示」→「AR（実寸）で使うモデル」で ★地面基準 を選ぶ
  2) 「AR（実寸）」→ AR点まわりの地面を映して 床を検出させる
  3) ★大きいピンクの箱（AR1）を AR1 の鋲に重ねる（ドラッグ）
  4) ★小さい箱が AR2〜AR6 の鋲に乗るまで 2本指で回す
  5) 触らない。歩いても ARKit が保つ
```

```
  ★高さの誤差 ＝ 置いた地面の標高 − 8.200
    AR点まわりの現況地盤高（横断図から）
      AR1 8.21 ／ AR2 8.11 ／ AR3 8.08 ／ AR4 8.44 ／ AR5 8.62 ／ AR6 8.72
    → AR1〜AR3 なら数 cm。AR5・AR6 側は 0.4〜0.5 m 高く出る
  ★実測した地面標高で作り直せる
      node scripts\sd_usdz.mjs 出力\Viewer\sd_model.json 出力\Viewer\model_ground.usdz --datum 8.35
  ★datum 8.200 だと 大型張ブロック・基礎・均し・根固め・かごマットは消える
    （8.2 より下）。護岸まで見たいときは datum 7.50 くらいにするか 全体版を使う
```

### ★Quick Look のジェスチャー（高さも動かせる・S38）

```
  1本指ドラッグ    平面位置を動かす。★検出した面に吸着する（別の面へも移る）
  ★2本指を上へドラッグ  ★モデルを浮かせる（levitate）。高さを動かせる
  2本指を回す      向きを変える
  ピンチ           ★拡大縮小できてしまう（実寸が崩れる）
  ダブルタップ     初期状態に戻る（拡大も向きも戻るので 合わせた後は押さない）
```

★つまり「下端が床に乗る」のは ★置いた瞬間の話で、後から上げられる。

```
  ★おすすめの順番（順番を間違えると やり直しになる）
    1) 置く（1本指ドラッグで だいたいの位置へ）
    2) 2本指を回して 向きを合わせる（遠い鋲の十字が その鋲に乗るまで）
    3) 1本指ドラッグで 平面位置を詰める（AR1 の十字の交点を AR1 の鋲へ）
    4) ★最後に 2本指を上へドラッグして 高さを合わせる
       （近くの十字が その鋲に乗るまで。★川表なら AR7〜AR9 の十字が分かりやすい）
    5) ★以後 触らない。1本指で動かすと 高さが床に戻ることがある
  ★注意  ピンチが同じ2本指に割り当たっている。上げるときは
         2本指の間隔を変えないようにする。画面の縮尺表示が 100% か時々確認する
```

```
  ★これができるなら 全体版のままでも XYZ を合わせられる。
    datum 版（天端で置く／水際で置く）は「最初から近い高さで出るので楽」という位置づけ
```

### ★Quick Look は置いた瞬間 モデルが 4〜5 m 頭上に出る（後から下げられる・S38）### ★Quick Look はモデルが 4〜5 m 頭上に出る（高さは合わない）

```
  モデルの標高範囲  4.005〜11.427 m（均しコンの下端 〜 AR1 のポール頭）
  AR基準点（天端）  8.427〜9.078 m
  ★Quick Look は モデルの最下端を「検出した床」に乗せる。
    最下端は 河床の均しコン。天端はそこから ★4.4〜5.1 m 上
  → 平場に立って置くと 天端が 4〜5 m 頭上に出る。★これは正常
  ★Quick Look は 高さを数値で決められない（ドラッグは水平だけ）。
    高さを合わせたいなら
      ・河床の高さに近い所（掘削面・低い所）に立って置く
      ・または ★高さが要る確認は「現地合わせ」でやる（標高は絶対値で正しい）
```

### Quick Look（ポール方式）は iPad 9 だと少し不利

```
  ・LiDAR が無いので平面検出がカメラ画像頼み。草地や均一な砕石だと床を拾いにくい
    → 足元をゆっくり左右に振って、模様のある地面（砕石の粒・目地・影）を映す
  ・★オクルージョンが効かない。実物の陰にモデルが隠れず、常に手前に浮いて見える
  ・特徴点が少ない場面ではトラッキングが飛ぶことがある。ゆっくり歩く
```

### 現場に出る前にやっておくこと

```
  1) ★事務所の Wi-Fi でビューアを一度開く（sw.js が全ファイルをキャッシュする）
     Wi-Fi モデルは現場で回線が無い。開いていないと現地で何も出ない
     ★更新した直後は 必ず開き直す（キャッシュ版数 sd-viewer-v2）
     ※現場は iPhone のテザリングでよいが、事前にキャッシュしておけば
       テザリングが切れても動く。インターネット共有は画面消灯で切れることがある
  2) ホーム画面に追加しておく（PWA として開く。アドレスバーが消えて見やすい）
  3) 設定 → Safari → モーションと画面の向きへのアクセス をオン
     （これがオフだと「現地合わせ」で姿勢が取れない）
  4) 画面を最大輝度に。10.2インチは直射日光だと見づらいのでフード／日よけがあると楽
```

## ★なぜ Quick Look と SketchUp で差があるのか（S36）

```
  同じ ARKit を使っている。差は「その上に載っているソフト」
  AR Quick Look    ★iOS の標準機能。USDZ を表示するだけで UI は Apple が固定。
                   レイヤ切替・配置基準の変更・数値入力は無い。
                   ★USDZ 自体にタグ／レイヤの概念が無い（COLLADA と同じ・§8-14）
                   配置は ★バウンディングボックスの底 を平面に乗せる固定仕様
  SketchUp Viewer  ★専用アプリ。.skp のタグ・シーン・断面がそのまま生きる。
                   AR の配置 UI も自前なので 基準の取り方を自由に作れる

  Android でも  Scene Viewer（glTF）は同じく固定 UI ＝ 差は埋まらない
  ★埋まるのは 自作 web の方。Android の Chrome は WebXR に対応しているので、
    このビューアの中で AR ができる → 鋲基準の配置・部品の切替・高さ入力が全部使える
    （iOS の Safari は WebXR 非対応。同じことをするならネイティブアプリが要る）
```

## 現地 AR の使い方（Android Chrome）

```
  1) 右上の「AR」を押す
  ★「基準の杭」で AR1〜AR10（鋲）を選べる（S36）。
    その鋲の上に輪郭を出して ①、別の鋲に向けて ② → 座標どおりに置ける。
    高さは「高さ」スライダー。★Quick Look と違い 下端基準ではない
  2) 床（地面）を映して、緑の輪が出るまで少し動かす
  3) 「基準の杭」で、いま自分が立っている杭を選ぶ（例 NO.99）
  4) 杭の足元に輪を合わせて「① 足元に合わせる」
  5) 堤防の上流側の地面に輪を向けて「② 上流へ向ける」
  6) ずれていれば「回転」「高さ」で微調整
```

★iPhone / iPad の Safari は WebXR AR に未対応です（2026年時点）。
　そのため iOS では上の AR Quick Look に自動で振り分けます。ボタンは同じです。
★モバイル PC に外部カメラをつないでも WebXR AR は動きません。
　Chrome の AR セッションは ARCore（Android 実機）が前提です。

## 注意

```
  ・座標系  JGD2011 平面直角座標系 第9系 ／ 標高 T.P.
  ・ローカル原点  X=84698.0  Y=78795.0
      X_JGD = モデルY + 84698.0 ／ Y_JGD = モデルX + 78795.0
  ・範囲  設計追加距離 1914.000〜2109.000（大型張ブロック区間 195.000 m）
  ・AR の位置合わせは手動です。座標での固定ではないので、
    出来形の判定には使えません。形と納まりの確認用です。
```


---

## ★現地で標高を入れる（S43）

レベルで測った鋲の頭の標高は、**現地でアプリに直接入れられます**。CSV も 作り直し.bat も
GitHub 更新も要りません。

```
  設定パネル → 「★基準点の標高（現地で入れる）」
    ① AR1〜AR10 の欄に 実測標高（T.P. m）を入れる
    ② 「保存」
    → 現地合わせ（自作AR）に その場ですぐ効く。ポールと十字もその高さに動く
    ※ この端末のブラウザにだけ保存されます（localStorage）
```

```
  「書き出す」  いま入っている値を CSV にして 枠に出す＋クリップボードにコピー
                メール／AirDrop／メモ で PC に送る
  「貼り付けて読み込む」  枠に CSV を貼って押すと 読み込む
                （別の端末で入れた値を移すとき・PC で作った値を現地に持ち込むとき）
  「元値に戻す」  データに入っている値（＝USDZ と同じ値）に戻す
```

### AR（実寸／Quick Look）はどうなるか

USDZ は作り置きのファイルなので、**現地で入れた値ではその場では動きません。**
代わりに、同じパネルの下に「十字を鋲に重ねたあと 何 m 上げ下げするか」が出ます。

```
  例）AR7　モデルを 0.043 m 上げる
      → Quick Look で 十字を鋲に重ねたあと、2本指で上に 0.043 m ドラッグ
```

きちんと直すときは、書き出した CSV を PC の `出力\AR基準点_実測Z.csv` に貼って
`作り直し.bat` → GitHub 更新。これで USDZ も 全員のビューアも 実測値になります。
